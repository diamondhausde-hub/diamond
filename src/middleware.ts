import {NextResponse} from 'next/server'; import type {NextRequest} from 'next/server'; export function middleware(req:NextRequest){const {pathname}=req.nextUrl; if(pathname==='/'||pathname==='/de/'||pathname==='/en/') return NextResponse.next(); if(!pathname.startsWith('/de')&&!pathname.startsWith('/en')) return NextResponse.redirect(new URL('/de'+pathname,req.url)); return NextResponse.next()} export const config={matcher:['/((?!_next|api|.*\\..*).*)']};
