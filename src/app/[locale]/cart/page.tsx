'use client'; import {useState} from 'react'; import Link from 'next/link'; import de from '@/data/de.json'; import en from '@/data/en.json';
export default function Cart({params}:{params:{locale:string}}){const t=params.locale==='de'?de:en; const [sent,setSent]=useState(false); return <div className="container min-h-[520px] py-16"><p className="text-xs font-bold uppercase tracking-[.18em] text-diamond">DIAMOND</p><h1 className="display mt-4 text-6xl font-bold">{t.nav.cart}</h1>{sent?<div className="mt-10 max-w-xl border-l-4 border-diamond bg-slate-50 p-6"><p className="font-bold">{t.common.success}</p><Link href={`/${params.locale}/shop`} className="mt-4 inline-block text-sm font-bold text-diamond">{t.product.back} →</Link></div>:<form onSubmit={e=>{e.preventDefault();setSent(true)}} className="mt-10 grid max-w-2xl gap-4"><p className="text-slate-600">{params.locale==='de'?'Ihr Anfragekorb ist bereit. Teilen Sie uns Ihre Kontaktdaten und gewünschte Mengen mit.':'Your inquiry cart is ready. Share your details and requested quantities.'}</p><input required placeholder="Name *" className="border p-4"/><input required type="email" placeholder="E-Mail *" className="border p-4"/><textarea required rows={6} placeholder={params.locale==='de'?'Produkte und Mengen *':'Products and quantities *'} className="border p-4"/><button className="w-fit bg-diamond px-6 py-4 text-sm font-bold text-white">{t.common.send}</button></form>}</div>}
