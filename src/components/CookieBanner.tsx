'use client'; import {useState} from 'react'; import {X} from 'lucide-react'; import de from '@/data/de.json'; import en from '@/data/en.json';
export default function CookieBanner(){const [show,setShow]=useState(true); if(!show)return null; const t=de; return <div className="fixed bottom-4 left-4 z-50 flex max-w-xl items-center gap-4 border border-slate-300 bg-white p-4 text-xs shadow-xl"><p className="leading-5">{t.common.cookie}</p><button onClick={()=>setShow(false)} className="focus-ring shrink-0 bg-diamond px-4 py-2 font-bold text-white">{t.common.accept}</button><button onClick={()=>setShow(false)} aria-label="Schließen"><X size={16}/></button></div>}
